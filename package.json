{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "dev": "PORT=3000 BASE_PATH=/ pnpm --filter @workspace/fifa-dashboard run dev",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck",
    "test": "PORT=5000 node scripts/test-api.js",
    "test:all": "pnpm --filter @workspace/api-server run build && pnpm run test",
    "demo:check": "node scripts/demo-health-check.js",
    "demo:e2e": "pnpm --filter @workspace/api-server run build && node scripts/e2e-demo.js",
    "demo:e2e:quick": "node scripts/e2e-demo.js --intensity high --duration 60 --no-start-server"
  },
  "private": true,
  "devDependencies": {
    "prettier": "^3.8.3",
    "typescript": "~5.9.3"
  },
  "packageManager": "pnpm@9.10.0+sha1.216899f511c8dfde183c7cb50b69009c779534a8"
}
